import random
names = input('Enter the Names..').split(", ")
total = len(names)
payee = random.randint(0,total-1)
print(f"{names[payee]} is going to buy the meal today!")

name= "g a n"
print('total value:',len(name))

